name: Deployment

on:
  push:
    branches:
      - dev

jobs:
  tag_release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Bump version and push tag
        id: versioning
        uses: anothrNick/github-tag-action@1.71.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEFAULT_BUMP: auto
          WITH_V: true

  deployment:
    needs: [tag_release]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SSH_SERVER }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          script: |
            # set -e  # Exit on error

            # echo "Stopping Laravel server..."
            # tmux send-keys -t 1 C-c || true  # Stop Laravel in tmux
            # sleep 2 

            # echo "Stopping processes on port 8000..."
            # if lsof -t -i :8000; then
            #   kill -9 $(lsof -t -i :8000)
            # fi

            # cd /var/www/html/Reconcile-AI-BE

            # echo "Fetching latest changes..."
            # git fetch origin dev
            # git reset --hard origin/dev  # Avoid merge issues
            # git clean -df  # Remove untracked files

            # echo "Installing dependencies..."
            # composer install --no-interaction --prefer-dist --no-dev --quiet

            # echo "Running migrations..."
            # php artisan migrate --force

            # echo "Restarting services..."
            # sudo systemctl restart nginx
            # sudo systemctl restart php8.2-fpm

            # echo "Deployment complete."
            echo "Starting Laravel deployment service..."
            sudo systemctl daemon-reload
            sudo systemctl start laravel-deploy.service
            echo "Deployment triggered successfully!"